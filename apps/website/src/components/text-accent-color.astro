---
// <script>
//   import { color } from '@color-kit/crystal'
//   const root = document.querySelector(':root')

//   const setColor = ({ h, s, l, a }: Color) => {
//     root.style.setProperty('--sl-color-text-accent', `hsl(${h} ${s} ${l} / ${a})`)
//   }
//   setColor(color.get())
//   color.subscribe(value => {
//     setColor(value)
//   })
// </script>
---

<slot />
